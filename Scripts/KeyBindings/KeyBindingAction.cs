[System.Serializable]
public enum KeyBindingAction
{
    Shoot,          // suggested: Mouse0
    DeployMine,     // suggested: Mouse1
    MoveUp,         // suggested: W / ArrowUp
    MoveDown,       // suggested: S / ArrowDown
    MoveRight,      // suggested: D / ArrowRight
    MoveLeft,       // suggested: A / ArrowLeft
    Pause,          // suggested: Escape
    Scoreboard,     // suggested: Tab
}